---
# create_snapshot_vm.yml

- name: Create the snapshots for the list of hosts
  hosts: all
  vars_files:
  - vars/vault_vmware.yml
  - vars/vmware.yml
  vars: 
    current_state: present
  
  tasks:
  - include_tasks: snapshot_action.yml
    loop: "{{ snapshot_hosts }}"